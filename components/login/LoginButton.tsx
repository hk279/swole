import React from "react";

import Button from "../_generic/Button";

const LoginButton = () => {
    return <Button onClick={() => {}} text="Log In" primary={true} success={true} />;
};

export default LoginButton;
